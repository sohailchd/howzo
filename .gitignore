# python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.DS_Store

# local machine config (paths are per-user; see README for setup)
.mcp.json

# local scratch: benchmark/A-B scripts and their temp DB copies
.bench/

# environment & secrets (never commit)
.env
.env.*
*.env
.envrc
*.pem
*.key
*.p12
id_rsa*
id_ed25519*
.pypirc
*-key.json
*credentials*.json

# design notes stay local: they are working documents, not published pages
# (GitHub Pages renders docs/*.md into indexable HTML, which is not wanted here)
docs/design/
